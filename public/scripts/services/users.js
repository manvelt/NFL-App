// I believe that this file does not need to be here because I
// will be handling the user authentication on the backend.

// 'use strict';
//
// angular.module('NFLApp')
//   .service('userService', function($http) {
//     this.getUsers = function(cb) {
//       $http.get('api/users').then(cb);
//     };
//   });
